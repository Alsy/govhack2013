create table trade_year (
    total_trade     bigint,
    export_trade    bigint,
    import_trade    bigint,
    country         varchar(255),
    code            varchar(12),
    year            year
)
;
